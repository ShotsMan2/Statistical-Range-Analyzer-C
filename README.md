# Statistical-Range-Analyzer-C
A C-based statistical tool that processes an integer dataset to determine the statistical range (amplitude) by dynamically tracking extrema (minimum and maximum) values.
